# A set of commands to be use in the workshop

```
$ git init

$ git add

$ git commit

$ git status

$ git log

$ git show

$ git config user.email

$ git config user.name
```


For the second session


```
$ git clone

$ git push

$ git pull

$ git merge

$ git remote add

$ git fetch

$ git rebase
```
